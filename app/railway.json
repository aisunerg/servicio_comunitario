{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "numReplicas": 1,
        "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput --clear && python manage.py loaddata admin_interface_theme_biblioteca.json && gunicorn core.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}